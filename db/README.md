# Database Setup
- Run `schema.sql` on a fresh Postgres to initialize all tables.
- Optionally run `seed_data.sql` to insert test data.
- For schema changes, add a new file to `/migrations` and run in order.
